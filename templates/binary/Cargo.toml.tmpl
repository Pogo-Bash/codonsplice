[package]
name = "{{name}}"
version = "{{version}}"
edition = "2021"

[dependencies]
{{core_dep}}
serde_json = "1"

[[bin]]
name = "{{name}}"
path = "src/main.rs"

[profile.release]
opt-level = 3
lto = true
